pipeline {
    agent any

    stages {
        stage('Pull from GitHub') {
            steps {
                git 'https://github.com/Malaikakhattak69/Devops.git'
            }
        }

        stage('Build') {
            steps {
                bat 'echo Building the project...'
                // Add your build steps here (e.g., bat 'mvn package' or bat 'npm install')
            }
        }

        stage('Deploy') {
            steps {
                bat 'echo Deploying the project...'
                // Add your deploy logic here (e.g., bat 'copy ...', bat 'xcopy ...')
            }
        }
    }
}
